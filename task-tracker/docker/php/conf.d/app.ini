; Project-wide PHP settings.
date.timezone = UTC
memory_limit = 512M

; Dev-friendly opcache: compiled code is cached but changed files are picked up.
opcache.enable = 1
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0
